export interface Product {
    id: number,
    title: string,
    description: string,
    brand: string,
    category: string,
    discountPercentage: number,
    images: string[],
    price: string,
    rating: number,
    stock: number,
    thumbnail: string,
}